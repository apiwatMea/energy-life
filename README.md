
# ENERGY LIFE V3 (Web Game + Backend + Admin Dashboard)

เกมจำลองค่าไฟ + โค้ชประหยัดพลังงาน (เวอร์ชันใช้งานได้จริงแบบ MVP)
- Login / Register
- บ้านแบบเลือกได้ (ประเภทบ้าน/ขนาดบ้าน/จำนวนคน)
- อุปกรณ์ไฟฟ้าครบ (ตั้งค่า BTU/อุณหภูมิ/ชั่วโมง/วัตต์)
- โหมดมิเตอร์ Non‑TOU / TOU
- Solar (0/3/5/10 kW) แบบง่าย
- EV + Home Charger (คำนวณ kWh ต่อครั้ง)
- ระบบเตือน + คะแนน + ร้านค้า
- Dashboard รายบ้าน (ผู้เล่น)
- Admin Dashboard: ดูจำนวนคนเล่น + ตั้งค่าอัตราค่าไฟ + ดูรายบ้าน

> หมายเหตุ: โมเดลคำนวณและค่าไฟเป็น MVP (ปรับให้ตรงข้อมูลจริงได้ในหน้า Admin และในโค้ด)

---

## 1) ติดตั้งและรัน

### ต้องมี
- Python 3.10+ (แนะนำ)
- pip

### ขั้นตอน
```bash
cd energy_life_mvp
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS/Linux:
# source .venv/bin/activate

pip install -r requirements.txt
python app.py
```

เปิดเบราว์เซอร์: http://127.0.0.1:5000

---

## 2) บัญชี Admin เริ่มต้น
- user: `admin`
- pass: `admin1234`

**แนะนำเปลี่ยนทันที** โดยตั้ง Environment Variables ก่อนรัน
```bash
set ENERGY_LIFE_ADMIN_PASS=yourStrongPassword
set ENERGY_LIFE_ADMIN_USER=admin
python app.py
```

(macOS/Linux ใช้ `export` แทน `set`)

---

## 3) โฟลเดอร์สำคัญ
- `app.py` : Backend + API + DB
- `templates/` : หน้าเว็บ
- `static/` : CSS/JS
- `energy_life.db` : SQLite database (จะถูกสร้างอัตโนมัติ)

---

## 4) จุดต่อยอด (Next)
- ทำภาพการ์ตูนจริง (Sprites) และฉาก Cutaway บ้านแบบเต็ม
- ทำระบบ TOU schedule แบบรายชั่วโมง (hourly simulation)
- Solar Advisor อิงจังหวัด/ฤดูกาล/โหลดกลางวันจริง
- ระบบ Officer แยกองค์กร/โครงการนำร่อง
- Export รายงาน PDF/CSV สำหรับเจ้าหน้าที่


## V2 เพิ่มเติม
- Solar Mode: เลือกเอง หรือให้ระบบแนะนำ (Advisor)
- TOU แยก On/Off แบบรายชั่วโมงสำหรับแอร์และ EV (อุปกรณ์อื่นใช้ heuristic)
- ตั้งเวลาแอร์เริ่ม/หยุด (รองรับข้ามเที่ยงคืน)
- ตั้งเวลาเริ่มชาร์จ EV
- แสดงแถบ Progress เลเวลบ้าน


## New in V3
- 🎯 ภารกิจ (Missions) + รับรางวัล
- 🐾 สัตว์เลี้ยง + อาหารสัตว์จากแต้ม
- 🏆 อันดับประจำสัปดาห์ (Weekly Leaderboard)
- 👤 โปรไฟล์ + ไอเท็มตั๋วเปลี่ยนชื่อ
- 🔗 แชร์บ้านแบบลิงก์ดูอย่างเดียว
- ⚙️ ตั้งค่า (เสียง/มุมมอง/ภาษา/ความเป็นส่วนตัว)
